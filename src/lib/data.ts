
import type { Career, Question, NewsItem, Scholarship } from './types';

// This file is being deprecated. Data is moved to more specific files.
// We keep it here for now to avoid breaking imports that might have been missed.
// In a real project, this file would be deleted after confirming all imports are updated.

export const careers: Career[] = [];
export const quizQuestions: Question[] = [];
export const latestNews: NewsItem[] = [];
export const scholarships: Scholarship[] = [];

    